-- =====================================================================
-- Zigso — 기존 레거시 이메일 테스트 계정 정리 및 마스터 계정 보존
--
-- 구글 로그인 도입 및 이메일 회원가입 비활성화에 따라,
-- 기존에 일반 이메일로 가입했었던 불필요한 테스트 사용자 데이터(가비지 데이터)를 정리합니다.
-- 체험용 마스터 계정(test@example.com) 및 Google 로그인 유저는 보존합니다.
-- =====================================================================

DELETE FROM auth.users 
WHERE email != 'test@example.com' 
  AND raw_app_meta_data->>'provider' = 'email';
